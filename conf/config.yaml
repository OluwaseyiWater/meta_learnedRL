defaults:
  - env: default
  - maml: default
  - recurrent_ml: default
  - attention_recurrent_ml: default
  - ppo: default

  
seed: 42

# # conf/config.yaml
# # Improved MAML configuration for dynamic spectrum allocation

# defaults:
#   - _self_

# seed: 42

# env:
#   _target_: mLN.environment.DynamicSpectrumEnv
#   num_bs: 3
#   num_users: 10
#   num_bands: 5
#   max_steps: 100
#   max_latency: 50.0
#   max_power_dbm: 23.0
#   num_power_levels: 4
#   power_levels_dbm: [0.0, 7.67, 15.33, 23.0]  # Evenly spaced power levels
#   fading_coherence: 0.9
#   max_external_interference_mW: 25.0
#   min_sinr_db: 5.0
#   bandwidth_hz: 10000000.0  # 10 MHz
#   noise_figure_db: 7.0

# maml:
#   # Meta-learning parameters (improved)
#   num_meta_tasks: 8         # Increased from typical 4-6
#   num_meta_iterations: 2000 # Increased iterations
#   meta_lr: 0.001           # Reduced from typical 0.01
  
#   # Inner loop parameters (improved)
#   inner_lr: 0.01           # Reduced from typical 0.1
#   inner_steps: 5           # Increased from typical 1-3
  
#   # Evaluation parameters
#   eval_interval: 20        # More frequent evaluation
#   num_eval_tasks: 10       # More evaluation tasks
  
#   # Logging
#   wandb_project: "maml-spectrum-allocation-v2"
#   wandb_name: "improved_maml_run"
#   use_wandb: true

# # Hydra configuration
# hydra:
#   run:
#     dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
#   sweep:
#     dir: multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
#     subdir: ${hydra.job.num}
